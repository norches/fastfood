version: "3.8"

# Local-only conveniences: bind to localhost ports for debugging
services:
  db:
    ports:
      - "127.0.0.1:5432:5432"

  pgadmin:
    ports:
      - "127.0.0.1:5050:80"

  backend:
    ports:
      - "127.0.0.1:8080:8080"

  frontend:
    ports:
      - "127.0.0.1:5173:80"